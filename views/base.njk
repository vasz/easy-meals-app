<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>EasyMeals</title>
</head>
<body>
  {#
    Header logic:
    - If isSplashPage is true (set in the splash/home page), show the logo header.
    - Otherwise, import and call the dynamic header macro with page-specific arguments.
    Usage:
      In splash page: {% set isSplashPage = true %}
      In other pages: set sectionLabel, showVegetarian, vegetarianActive as needed.
  #}
  {% if isSplashPage %}

    <header class="brand-header" style="text-align:center;padding:0em 0;">
      <img src="/images/easy-meals-logo.jpg" alt="Easy Meals Logo" style="padding:0px;margin:0px;height:32px;" />
    </header>
  {% else %}
    {% import "./macros/header-macro.njk" as headerMacros %}
    {{ headerMacros.pageHeader(sectionLabel, showVegetarian, vegetarianActive) }}
  {% endif %}

  {# Main content block #}
  {% block content %}{% endblock %}

  {% include "includes/_footer.njk" %}
</body>


